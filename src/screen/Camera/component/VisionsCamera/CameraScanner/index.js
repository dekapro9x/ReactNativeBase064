import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const CameraScanner = () => {
    return (
        <View>
            <Text>CameraScanner</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default CameraScanner;
